<UserControl x:Class="Spikit.Views.Onboarding.Steps.HotkeyStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="Transparent">
    <UserControl.RenderTransform>
        <TranslateTransform Y="0"/>
    </UserControl.RenderTransform>
    <UserControl.Style>
        <Style TargetType="UserControl">
            <Style.Triggers>
                <Trigger Property="Visibility" Value="Visible">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="0" To="1" Duration="0:0:0.32"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                 From="12" To="0" Duration="0:0:0.32">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>

    <!-- Placeholder — la UI real (HotkeyCaptureField + radio PTT/Toggle + validación)
         se cablea en EP-3.5 (ticket 86ahb6c2w) + EP-3.6. -->
    <Grid MaxWidth="480" HorizontalAlignment="Center" VerticalAlignment="Top">
        <StackPanel>
            <TextBlock Text="Elegí tu hotkey"
                       Style="{StaticResource SpkTypeH1Style}"
                       Margin="0,0,0,8"/>
            <TextBlock Style="{StaticResource SpkTypeBodyStyle}"
                       Foreground="{DynamicResource SpkFgSecondaryBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,32"
                       Text="Lo vas a apretar para empezar a dictar desde cualquier app."/>

            <Border Background="{DynamicResource SpkBgSurfaceBrush}"
                    BorderBrush="{DynamicResource SpkBorderSubtleBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20">
                <StackPanel>
                    <TextBlock Style="{StaticResource SpkTypeBodySmStyle}"
                               Foreground="{DynamicResource SpkFgSecondaryBrush}"
                               Text="PLACEHOLDER · EP-3.5 / EP-3.6"
                               Margin="0,0,0,8"/>
                    <TextBlock Style="{StaticResource SpkTypeBodyStyle}"
                               Foreground="{DynamicResource SpkFgPrimaryBrush}"
                               TextWrapping="Wrap"
                               Text="Acá van: HotkeyCaptureField que graba la combinación, radio Push-to-talk vs Toggle (default PTT), validación contra hotkeys del sistema."/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
