<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ContextMenu / MenuItem / Separator de Spikit. Estilos implícitos (sin x:Key) →
         afectan a CUALQUIER ContextMenu del proyecto. Hoy el único caller es el TrayIcon
         pero queda listo para futuros menús contextuales sin más cableado.

         Filosofía: matchear la estética dark de la app (SpkBgElevatedBrush + corner radius
         + drop shadow propio) en vez del chrome gris default de Windows. Sin Mica/Acrylic
         (decisión EP-8 — pragmático, evita pelearse con el HwndSource del popup). -->

    <!-- Hover background del MenuItem. Pequeño realce sobre SpkBgElevatedBrush sin pegar
         a SpkBgBrandSolid (rojo) para que el menú no se sienta agresivo. -->
    <SolidColorBrush x:Key="SpkContextMenuItemHoverBrush" Color="#33FFFFFF" />

    <!-- ============ MenuItem ============ -->
    <Style TargetType="{x:Type MenuItem}">
        <Setter Property="FontFamily" Value="{StaticResource SpkFontGeist}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Foreground" Value="{DynamicResource SpkFgPrimaryBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="14,8,14,8" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type MenuItem}">
                    <Border x:Name="Bg"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource SpkRadiusSm}"
                            Margin="4,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Grid.Column="0"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1"
                                       Text="{TemplateBinding InputGestureText}"
                                       Foreground="{DynamicResource SpkFgMutedBrush}"
                                       FontFamily="{StaticResource SpkFontGeistMono}"
                                       FontSize="11"
                                       VerticalAlignment="Center"
                                       Margin="0,0,12,0" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bg" Property="Background"
                                    Value="{StaticResource SpkContextMenuItemHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource SpkFgMutedBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ Separator dentro del menú ============ -->
    <!-- Targeteado a {x:Static MenuItem.SeparatorStyleKey} para que aplique solo a los
         Separators que viven dentro de menús (ContextMenu / Menu) y no contamine separators
         de toolbars o paneles. -->
    <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="{x:Type Separator}">
        <Setter Property="Background" Value="{DynamicResource SpkBorderSubtleBrush}" />
        <Setter Property="Height" Value="1" />
        <Setter Property="Margin" Value="8,4,8,4" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Separator}">
                    <Border Background="{TemplateBinding Background}" Height="1" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============ ContextMenu ============ -->
    <Style TargetType="{x:Type ContextMenu}">
        <Setter Property="Background" Value="{DynamicResource SpkBgElevatedBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource SpkFgPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource SpkBorderDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HasDropShadow" Value="True" />
        <Setter Property="Padding" Value="0,4,0,4" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContextMenu}">
                    <!-- Contenedor exterior con padding para que el DropShadow no se corte
                         contra el borde físico del popup. WPF setea AllowsTransparency=True
                         en el popup del ContextMenu cuando HasDropShadow=True, así que el
                         padding queda transparente. -->
                    <Grid Margin="12">
                        <Border x:Name="Shell"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource SpkRadiusMd}"
                                Effect="{StaticResource SpkShadowMd}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer CanContentScroll="True"
                                          VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Disabled">
                                <ItemsPresenter Margin="{TemplateBinding Padding}"
                                                KeyboardNavigation.DirectionalNavigation="Cycle"
                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
